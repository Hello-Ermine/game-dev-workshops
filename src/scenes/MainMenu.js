import Phaser from "phaser";

class MainMenu extends Phaser.Scene {
    constructor(test) {
        super({
            key: 'MainMenu'
        });
    }

    preload() {
     
    }

    create() {
       
    }

    update(delta, time) {
       
    }
}
export default MainMenu;
